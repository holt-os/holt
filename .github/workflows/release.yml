name: Release

# Fires when a version tag is pushed (scripts/release.sh does that). It verifies
# the tag matches the code, publishes to npm with an automation token (no passkey
# prompt), then bumps the Homebrew formula in holt-os/homebrew-tap.
#
# Required repo secrets:
#   NPM_TOKEN            npm "Automation" access token (bypasses 2FA on publish)
#   HOMEBREW_TAP_TOKEN   a token with contents:write on holt-os/homebrew-tap
on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: read

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: 'https://registry.npmjs.org'
          cache: npm

      - name: Verify tag matches package.json and version.ts
        run: |
          TAG="${GITHUB_REF_NAME#v}"
          PKG=$(node -p "require('./package.json').version")
          if [ "$TAG" != "$PKG" ]; then
            echo "Tag $GITHUB_REF_NAME does not match package.json version $PKG"
            exit 1
          fi
          if ! grep -q "VERSION = '$PKG'" src/version.ts; then
            echo "src/version.ts does not declare version $PKG"
            exit 1
          fi
          echo "VERSION=$PKG" >> "$GITHUB_ENV"

      - name: Install
        run: npm ci

      - name: Build
        run: npm run build

      - name: Typecheck
        run: npm run typecheck

      - name: Smoke test the CLI reports the release version
        run: |
          OUT=$(node dist/cli.js version)
          echo "$OUT"
          [ "$OUT" = "holt $VERSION" ] || { echo "CLI reported '$OUT', expected 'holt $VERSION'"; exit 1; }

      - name: Guard against em-dashes
        run: |
          if grep -rnP '\x{2014}' src README.md ARCHITECTURE.md CONTRIBUTING.md CONFIGURATION.md; then
            echo "em-dash found"
            exit 1
          fi
          echo "no em-dashes found"

      - name: Publish to npm
        # Skip when this version is already on npm, so re-running a run that
        # died in a later step can actually recover. Without this the re-run
        # republishes, npm answers E403, and the Homebrew bump stays
        # permanently unreachable for that version.
        run: |
          set -euo pipefail
          if npm view "@holt-os/holt@${VERSION}" version >/dev/null 2>&1; then
            echo "@holt-os/holt@${VERSION} is already published. Skipping publish."
          else
            npm publish --access public
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Checkout Homebrew tap
        uses: actions/checkout@v4
        with:
          repository: holt-os/homebrew-tap
          token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          path: tap

      - name: Bump the Homebrew formula
        run: |
          set -euo pipefail
          URL="https://registry.npmjs.org/@holt-os/holt/-/holt-${VERSION}.tgz"
          # npm updates the registry metadata before the CDN serves the tarball.
          # That gap was 72s of headroom against a real wait of about six
          # minutes on 0.18.0, which failed the release after a successful
          # publish. 10 minutes of headroom, and say so plainly when it runs out.
          for i in $(seq 1 60); do
            if curl -fsSL "$URL" -o /tmp/holt.tgz; then break; fi
            echo "waiting for npm tarball ($i/60)..."
            sleep 10
          done
          if [ ! -s /tmp/holt.tgz ]; then
            echo "::error::npm never served ${URL} after 10 minutes."
            echo "::error::${VERSION} IS published to npm. Only the Homebrew bump is missing."
            echo "::error::Do not re-run this workflow: it republishes first and npm rejects that with E403."
            echo "::error::Bump tap/Formula/holt.rb by hand with the url above and its sha256."
            exit 1
          fi
          SHA=$(shasum -a 256 /tmp/holt.tgz | awk '{print $1}')
          echo "url=$URL"
          echo "sha256=$SHA"
          F=tap/Formula/holt.rb
          sed -i -E "s#^  url \".*\"#  url \"${URL}\"#" "$F"
          sed -i -E "s#^  sha256 \".*\"#  sha256 \"${SHA}\"#" "$F"
          grep -E '  (url|sha256) ' "$F"

      - name: Commit and push the formula bump
        run: |
          set -euo pipefail
          cd tap
          if git diff --quiet; then
            echo "Formula already at ${VERSION}; nothing to push."
            exit 0
          fi
          git config user.name "Debashis Nayak"
          git config user.email "debashis.deb94@gmail.com"
          git add Formula/holt.rb
          git commit -m "holt ${VERSION}"
          git push
