/**
 * Stub replaced by the protocol engine on feat/mcp-server. Kept here so the
 * tools branch compiles against the real signature. Do not rely on this body.
 */
import type { McpServerOptions } from './types';

export async function runMcpServer(_opts: McpServerOptions): Promise<void> {
  throw new Error('runMcpServer not implemented yet');
}
